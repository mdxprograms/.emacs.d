;;; package --- clojure.el

;;; Commentary:

;;; Code:
;;; packages
(use-package clojure-mode
  :ensure t)

(use-package clojure-mode-extra-font-locking
  :ensure t)

(use-package cider
  :ensure t)

(provide 'clojure)
